{% extends 'secondsite/base.html.twig' %}

{% block title %}User2{% endblock %}

{% block body %}
    <h1>User2</h1>

    <table class="table">
        <tbody>
        <tr>
            <th>Id</th>
            <td>{{ user2.id }}</td>
        </tr>
        <tr>
            <th>Email</th>
            <td>{{ user2.email }}</td>
        </tr>
        <tr>
            <th>Roles</th>
            <td>{{ user2.roles ? user2.roles|json_encode : '' }}</td>
        </tr>
        <tr>
            <th>Password</th>
            <td>{{ user2.password }}</td>
        </tr>
        </tbody>
    </table>

    <a href="{{ path('users_index') }}">back to list</a>

    <a href="{{ path('user_edit', {'id': user2.id}) }}">edit</a>

    {{ include('/secondsite/user2/_delete_form.html.twig') }}
{% endblock %}
