{% extends 'secondsite/base.html.twig' %}

{% block title %}Edit Ad{% endblock %}

{% block body %}
   <div class="">
         <h1>Modifier</h1>
    {{ include('secondsite/ad/_form.html.twig', {'button_label': 'Update'}) }}

    <a class="btn btn-secondary mt-2" href="{{ path('ad_index') }}">back to list</a>

    <p> {{ include('secondsite/ad/_delete_form.html.twig') }}</p>
   </div>
{% endblock %}
